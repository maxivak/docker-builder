common({
    'prefix' => "example-",
    'image_prefix' => 'example-',
    'dir_data' => '/path/to/data/',

})

servers({
    '<%=@name%>'=>{
        # some server options here
    }

})


base({

})
